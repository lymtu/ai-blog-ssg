# Dependencies
node_modules/
frontend/node_modules/

# Secrets & local env
.env
.env.*.local

# OS / IDE
.DS_Store
Thumbs.db
*.log
*.pem

# Agent / reference (not used in deploy)
.agents/
refer/
skills-lock.json

# Generated static output (frontend build + SSG; rebuilt in Docker / on start)
public/**
!public/data/
!public/data/.gitkeep
!public/assets/
!public/assets/uploads/
!public/assets/uploads/.gitkeep

# Alternate lockfiles (project uses bun.lock)
package-lock.json
pnpm-lock.yaml
frontend/pnpm-lock.yaml
yarn.lock

# Archives & traces
*.zip
*.tar.gz
*.tgz
**/*.trace

# Legacy / unused
.vercel
/.next/
/out/
/build
/.pnp
.pnp.js
/coverage
