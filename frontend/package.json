{
  "name": "blog-ssg-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "bun run scripts/generate-avatar-bin.ts",
    "dev": "vite",
    "prebuild": "bun run scripts/generate-avatar-bin.ts",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.35.2",
    "typescript": "^5.9.2",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "lit": "^3.3.2",
    "mermaid": "^11.15.0"
  }
}
